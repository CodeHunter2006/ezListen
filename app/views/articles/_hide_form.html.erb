<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "请输入网址或需要朗读的文本：" %><br>
    <%= f.text_area :source_url %>
  </div>
  <div class="field">
		<%= hidden_field(:article, :title) %>
		<%= hidden_field(:article, :text_content) %>
		<%= hidden_field(:article, :local_audio) %>
		<%= hidden_field(:article, :audio_file_url) %>
		<%= hidden_field(:article, :status) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
